#include "hal/util.h"
#include "hal/gpio.h"

volatile gpiox_t gpioa PERIPHERAL(gpioa);
volatile gpiox_t gpiob PERIPHERAL(gpiob);
volatile gpiox_t gpioc PERIPHERAL(gpioc);
volatile gpiox_t gpiod PERIPHERAL(gpiod);
volatile gpiox_t gpiof PERIPHERAL(gpiof);